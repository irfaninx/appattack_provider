import 'package:flutter/material.dart';

class nearby_view extends StatefulWidget {
  const nearby_view({super.key});

  @override
  State<nearby_view> createState() => _nearby_viewState();
}

class _nearby_viewState extends State<nearby_view> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
